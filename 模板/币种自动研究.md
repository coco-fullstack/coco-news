<%*
const query = await tp.system.prompt("输入币种名称（如 kite, bitcoin, ethereum）");
if (!query) return;

try {
  // 搜索币种
  const searchUrl = `https://api.coingecko.com/api/v3/search?query=${encodeURIComponent(query)}`;
  const searchRes = await requestUrl({url: searchUrl});
  const coins = searchRes.json.coins;

  if (!coins || coins.length === 0) {
    new Notice("未找到该币种，请检查名称");
    return;
  }

  const coin = coins[0];
  const coinId = coin.id;
  const coinSymbol = coin.symbol.toUpperCase();
  const coinName = coin.name;

  // 获取详细数据
  const dataUrl = `https://api.coingecko.com/api/v3/coins/${coinId}?localization=false&tickers=false&community_data=true&developer_data=true`;
  const dataRes = await requestUrl({url: dataUrl});
  const d = dataRes.json;

  // 基础数据
  const price = d.market_data?.current_price?.usd != null ? d.market_data.current_price.usd : "N/A";
  const mcap = d.market_data?.market_cap?.usd ? "$" + (d.market_data.market_cap.usd / 1e6).toFixed(2) + "M" : "待更新";
  const fdv = d.market_data?.fully_diluted_valuation?.usd ? "$" + (d.market_data.fully_diluted_valuation.usd / 1e6).toFixed(2) + "M" : "待更新";
  const vol = d.market_data?.total_volume?.usd ? "$" + (d.market_data.total_volume.usd / 1e6).toFixed(2) + "M" : "N/A";

  // 涨跌幅
  const c1h = d.market_data?.price_change_percentage_1h_in_currency?.usd != null ? d.market_data.price_change_percentage_1h_in_currency.usd.toFixed(2) + "%" : "N/A";
  const c24h = d.market_data?.price_change_percentage_24h != null ? d.market_data.price_change_percentage_24h.toFixed(2) + "%" : "N/A";
  const c7d = d.market_data?.price_change_percentage_7d != null ? d.market_data.price_change_percentage_7d.toFixed(2) + "%" : "N/A";
  const c30d = d.market_data?.price_change_percentage_30d != null ? d.market_data.price_change_percentage_30d.toFixed(2) + "%" : "N/A";

  // 供应量
  const circSupply = d.market_data?.circulating_supply ? (d.market_data.circulating_supply / 1e6).toFixed(2) + "M" : "N/A";
  const totalSupply = d.market_data?.total_supply ? (d.market_data.total_supply / 1e6).toFixed(2) + "M" : "N/A";
  const maxSupply = d.market_data?.max_supply ? (d.market_data.max_supply / 1e6).toFixed(2) + "M" : "无上限";
  const circRate = (d.market_data?.circulating_supply && d.market_data?.total_supply) ? (d.market_data.circulating_supply / d.market_data.total_supply * 100).toFixed(1) + "%" : "N/A";

  // 历史极值
  const ath = d.market_data?.ath?.usd != null ? "$" + d.market_data.ath.usd : "N/A";
  const athDate = d.market_data?.ath_date?.usd ? d.market_data.ath_date.usd.split("T")[0] : "";
  const athDrop = d.market_data?.ath_change_percentage?.usd != null ? d.market_data.ath_change_percentage.usd.toFixed(1) + "%" : "";
  const atl = d.market_data?.atl?.usd != null ? "$" + d.market_data.atl.usd : "N/A";
  const atlDate = d.market_data?.atl_date?.usd ? d.market_data.atl_date.usd.split("T")[0] : "";

  // 链接
  const website = d.links?.homepage?.[0] || "";
  const twitter = d.links?.twitter_screen_name ? "https://x.com/" + d.links.twitter_screen_name : "";
  const telegram = d.links?.telegram_channel_identifier ? "https://t.me/" + d.links.telegram_channel_identifier : "";
  const github = d.links?.repos_url?.github?.[0] || "";
  const discord = d.links?.chat_url?.find(u => u.includes("discord")) || "";

  // 其他
  const categories = d.categories?.filter(c => c)?.join(", ") || "待分类";
  const rank = d.market_cap_rank || "N/A";
  const genesis = d.genesis_date || "未知";
  const desc = d.description?.en ? d.description.en.replace(/<[^>]*>/g, '').split('. ').slice(0, 3).join('. ') + '.' : "待补充";
  const twitterFollowers = d.community_data?.twitter_followers ? (d.community_data.twitter_followers / 1000).toFixed(1) + "K" : "N/A";

  // 构建实时图表 URL
  const tvSymbol = coinSymbol + "USDT";
  const cgUrl = `https://www.coingecko.com/en/coins/${coinId}`;

  await tp.file.rename(coinName);

  tR += `---
title: ${coinName}
date: ${tp.date.now("YYYY-MM-DD")}
tags:
  - research/token
  - token/${coinSymbol}
related: []
status: active
---

## 基本信息

| 项目 | 内容 |
|------|------|
| 全称 | ${coinName} |
| 代币 | ${coinSymbol} |
| 排名 | #${rank} |
| 赛道 | ${categories} |
| 上线日期 | ${genesis} |

## 链接

- 官网：${website}
- Twitter：${twitter}（粉丝 ${twitterFollowers}）
- Telegram：${telegram}
- GitHub：${github}
- Discord：${discord}

## 项目简介

${desc}

## 实时行情

> [TradingView K线图 →](https://www.tradingview.com/chart/?symbol=BINANCE:${tvSymbol})  |  [CoinGecko →](${cgUrl})  |  [Binance →](https://www.binance.com/zh-CN/trade/${tvSymbol})

## 快照数据（${tp.date.now("YYYY-MM-DD")} 创建时获取）

### 价格 & 市值

| 指标 | 数值 |
|------|------|
| 价格 | $${price} |
| 市值 | ${mcap} |
| FDV | ${fdv} |
| 24h 成交量 | ${vol} |
| 市值排名 | #${rank} |

### 涨跌幅

| 1h | 24h | 7d | 30d |
|----|-----|----|-----|
| ${c1h} | ${c24h} | ${c7d} | ${c30d} |

### 供应量

| 指标 | 数值 |
|------|------|
| 流通量 | ${circSupply} |
| 总供应 | ${totalSupply} |
| 最大供应 | ${maxSupply} |
| 流通率 | ${circRate} |

### 历史极值

| 指标 | 价格 | 日期 | 距今 |
|------|------|------|------|
| ATH（历史最高） | ${ath} | ${athDate} | ${athDrop} |
| ATL（历史最低） | ${atl} | ${atlDate} | |

## 核心逻辑

- **解决什么问题**：
- **竞争优势**：
- **代币用途**：gas / 治理 / 质押 / 其他
- **代币经济模型**：

## 竞品对比

| 项目 | 代币 | 市值 | 特点 | 对比 |
|------|------|------|------|------|
| ${coinName} | ${coinSymbol} | ${mcap} | | |
| | | | | |
| | | | | |

## 风险评估

- [ ] 团队背景调查
- [ ] 代币解锁计划
- [ ] 合约是否审计
- [ ] 竞品对比完成
- [ ] 社区活跃度观察
- [ ] 大户持仓集中度

## 我的判断

- **评级**：关注 / 建仓 / 观望 / 回避
- **理由**：
- **目标价**：
- **止损价**：
- **仓位计划**：

## 操作记录

| 日期 | 操作 | 价格 | 数量 | 备注 |
|------|------|------|------|------|
| | 买入/卖出 | | | |

## 跟踪笔记

### ${tp.date.now("YYYY-MM-DD")}
- 首次建档
`;

} catch (e) {
  new Notice("获取数据失败：" + e.message);
}
%>
